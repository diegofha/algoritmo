#include <iostream>
#include <conio.h>

using namespace std;


void main(){
	int nro,r;
	cout<<"introducir numero ";
	cin>>nro;
	r= sumardigitosprimo(nro);
	cout<<"la suma de digitos primos de "<<nro<< " ,es --->"<<r;
	getch();
}
int nro,
bool k=true;
while(n>0){
	nro=n%10;
	if(nro>mayor){
		mayor=nro;}
	else{
		k=false;}
	n=n/10;
}
return(k)}
